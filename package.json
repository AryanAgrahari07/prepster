{
  "name": "prepster",
  "version": "1.0.0",
  "description": "Prepster monorepo — run frontend and backend together",
  "scripts": {
    "dev": "concurrently --names \"API,WEB\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed:all --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
